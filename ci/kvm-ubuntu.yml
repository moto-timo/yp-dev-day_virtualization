# yaml-language-server: $schema=https://raw.githubusercontent.com/siemens/kas/master/kas/schema-kas.json

header:
  version: 14
  includes:
    - ci/kvm.yml

local_conf_header:
  ubuntu_cloud_image: |
    IMAGE_INSTALL:append = " ubuntu-kvm-image-package"
